# Interview Coach — Web App (FastAPI + HTML/CSS + Voice Input)

Веб-приложение для тренировки интервью: один экран с вопросом, фидбеком и прогрессом. Клиент работает полностью в браузере, сервер — FastAPI + websockets. Генерация и оценка ответов делаются через OpenRouter LLM.

## 🚀 Быстрый старт

1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Создайте файл `.env` в корне проекта и добавьте API-ключ OpenRouter:
   ```
   OPENROUTER_API_KEY=ваш_ключ
   ```

3. Запустите сервер:
   ```bash
   uvicorn app.main:app --reload
   ```

4. Откройте в браузере: http://127.0.0.1:8000

## 📝 Как работать с системой

### Подготовка контента
1. Создайте папку `content/` в корне проекта
2. Добавьте файлы с вопросами в формате Markdown
3. Формат файла:
   ```markdown
   # Название темы
   
   ### Подтема
   
   ? Вопрос 1
   
   ! Ответ на вопрос 1
   
   ? Вопрос 2
   
   ! Ответ на вопрос 2
   ```

### Режимы работы

#### 🔄 Простой режим
- Последовательный перебор вопросов
- Кнопки управления:
  - **Далее** - перейти к следующему вопросу
  - **Пропустить** - отложить вопрос на повторение
- Вопросы из очереди повторов будут показаны после основного прохода

#### 🧠 Умный режим
- Адаптивный подбор вопросов на основе ваших ответов
- Использует ИИ для анализа и уточнения ответов
- Автоматически определяет слабые места

## 🔧 Технические детали

### Требования
- Python 3.9+
- Зависимости из `requirements.txt`
- API ключ OpenRouter

### Структура проекта
```
.
├── app/
│   ├── static/      # Фронтенд (HTML, CSS, JS)
│   └── main.py      # Бэкенд на FastAPI
├── content/         # Файлы с вопросами
├── .env             # Конфигурация
└── requirements.txt # Зависимости
```

### Доступные эндпоинты
- `GET /` - Веб-интерфейс
- `WS /ws` - WebSocket для обмена сообщениями
- `GET /api/sources` - Список доступных источников

## 📚 Дополнительно
- Для работы с голосовым вводом требуется микрофон
- Все ответы сохраняются локально в браузере
- Для перезапуска тестирования нажмите "Новая сессия".
- Контент: `interview_coach_core/parsing.py` читает Markdown в `NOTES_DIR`, режет по заголовкам и вытаскивает вопросы/теорию в `content_index`.
- Промпты и схемы: `interview_coach_core/prompts.py` хранит системные промпты для генерации фидбека/аналитики; `interview_coach_core/models.py` описывает ожидаемый JSON (rubric, topic_scores и т.п.).
- STT: `/stt` использует faster-whisper (модель, девайс и precision настраиваются через env).
- Фронтенд: статические файлы в `app/static/` (`index.html`, `style.css`, `app.js`). Сообщения рендерятся через marked.js + DOMPurify; голосовой ввод — MediaRecorder → POST `/stt` (faster-whisper).

## Быстрый старт
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```
Открыть http://localhost:8000. Для голосового ввода нужен браузер с MediaRecorder (Chrome/Edge). STT идёт через faster-whisper на сервере.

## Архитектура
- Backend: `app/main.py` поднимает FastAPI, статические файлы и websocket `/ws`. LLM клиенты собираются в `interview_coach_core/openrouter.py`.
- Core-логика: `app/coach_engine.py` управляет сессией, задаёт вопросы, шлёт статусы и оценивает ответы. Прогресс/анализ хранится в `SessionState`.
- Контент: `interview_coach_core/parsing.py` читает Markdown в `NOTES_DIR`, режет по заголовкам и вытаскивает вопросы/теорию в `content_index`.
- Промпты и схемы: `interview_coach_core/prompts.py` хранит системные промпты для генерации фидбека/аналитики; `interview_coach_core/models.py` описывает ожидаемый JSON (rubric, topic_scores и т.п.).
- STT: `/stt` использует faster-whisper (модель, девайс и precision настраиваются через env).
- Фронтенд: статические файлы в `app/static/` (`index.html`, `style.css`, `app.js`). Сообщения рендерятся через marked.js + DOMPurify; голосовой ввод — MediaRecorder → POST `/stt` (faster-whisper).

## Как идет запрос
1) Клиент открывает `/` и устанавливает websocket `/ws`.
2) Сервер шлёт `hello` (модель, analyze_every_n, пути логов), затем `question`.
3) Пользователь вводит ответ (текст или голос), клиент отправляет `answer`.
4) `CoachEngine.submit_answer` собирает промпт из вопроса, expected (рубрика) и контекста, зовёт `llm_eval` с `EVAL_SYSTEM`. Ответ парсится через `robust_json_load`.
5) Фидбек (`feedback`) уходит в UI; счётчик вопросов растёт. Каждые N ответов — `run_analysis` с `ANALYSIS_SYSTEM`, summary падает в ленту.
6) Новые вопросы приходят после каждого ответа или сброса сессии.

## Формат контента
- Конспекты — Markdown файлы в `NOTES_DIR`. Заголовки `#/##/###` формируют темы. Внутри секции автоматически вытаскиваются вопросы (строки с `?`, `Q:` и т.п.).
- Для каждой темы сохраняется: `title`, `source_file`, `theory` (текст секции), `questions` (список {q, expected=None}).
- Если в секции нет явных вопросов, генератор создаёт их на лету. Если нет чеклиста expected, он генерируется как JSON-рубрика (см. промпт в `coach_engine.py`).

## Важные файлы
- `app/main.py` — сервер, websocket протокол.
- `app/coach_engine.py` — сессии, статусы, оценка, анализ, запись в историю.
- `interview_coach_core/prompts.py` — системные промпты для Evaluator/Analyzer.
- `interview_coach_core/models.py` — pydantic-схемы ожидаемых ответов моделей.
- `interview_coach_core/parsing.py` — загрузка конспектов, построение `content_index`.
- `app/static/*` — фронтенд (UI, стили, рендеринг Markdown, голос).

## Конфигурация (env)
- `OPENROUTER_API_KEY` — обязательный ключ.
- `OPENROUTER_MODEL` — модель для генерации/оценки (по умолчанию deepseek/deepseek-chat).
- `NOTES_DIR` — каталог с конспектами (по умолчанию `./conspects`).
- `RUNS_DIR` — каталог для логов сессий (`runs/<ts>/history.jsonl` + snapshot тем).
- `ANALYZE_EVERY_N` — периодичность анализа прогресса (по умолчанию 30).
- `MAX_QUESTIONS_PER_SOURCE` — лимит вопросов на один файл конспекта (0 = без лимита).
- `APP_HTTP_REFERER`, `APP_TITLE`, `LLM_TIMEOUT_S` — метаданные/таймауты для OpenRouter.
- `WHISPER_MODEL` (по умолчанию medium), `WHISPER_DEVICE`, `WHISPER_COMPUTE_TYPE` (int8_float16 для скорости), `WHISPER_INITIAL_PROMPT`, `WHISPER_LANGUAGE` ("" = авто), `WHISPER_BEAM_SIZE` (по умолчанию 2), `WHISPER_CONDITION_ON_PREV` — настройки faster-whisper.

## Логи и история
- Для каждой сессии создается каталог `runs/<timestamp>/`:
  - `history.jsonl` — последовательность вопросов/ответов/оценок.
  - `topics_snapshot.json` — список тем (id, title, source_file) на момент старта.
- В UI история сессии дублируется в правом debug drawer.

## Работа с фронтендом
- Все клиентские файлы лежат в `app/static/`. При изменении JS/CSS достаточно перезагрузить страницу.
- Лента сообщений и вопрос рендерятся как Markdown; формулы в `$...$/$$...$$` подсвечиваются простым CSS.
- Голосовой ввод: кнопка 🎙️/⏺️ меняет класс `.listening`, состояние показывает подсветкой.

## Разработка и отладка
- Запуск: `uvicorn app.main:app --reload`. Websocket переподключения не требуется — reload сам обновит backend.
- Нет миграций/БД: всё stateful на файловой системе в `runs/`.
- При проблемах с JSON-ответами LLM: смотри события в debug drawer и `history.jsonl`; `robust_json_load` вырезает первый JSON-блок, а при поломке отправляется recovery-промпт.

## Ограничения и идеи для улучшения
- Нет бекенда для аутентификации/мульти-пользователей — предполагается локальный запуск.
- Парсинг конспектов простой (по заголовкам); для сложной структуры можно расширить `parsing.py`.
- Для точной оценки лучше держать чеклисты/рубрики в конспектах заранее, чтобы минимизировать LLM-догадки.
