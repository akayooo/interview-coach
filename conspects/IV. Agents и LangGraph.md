Хендбук - база знаний по агентным системам и знаичмым в этой теме tools'ам

---
## 1. Фундаментальная теория агентов

### 1.1. От Chain к Agent: Эволюция Control Flow

Традиционное программирование и ранние LLM-приложения строились на базе жестких цепочек (Chains).

- **Chain (Цепочка):** Это Directed Acyclic Graph (DAG), где структура вызовов жестко задана программистом (`Prompt` $\to$ `LLM` $\to$ `Parser`).
    
    - _Проблема:_ Хрупкость. Если LLM вернет неожиданный формат или потребует уточнения, цепочка упадет. Она не умеет адаптироваться.
        
- **Agent (Агент):** Это система, где LLM берет на себя роль "Reasoning Engine". Агент работает в цикле (Loop). На каждом шаге он оценивает текущее состояние и принимает решение: "Вызвать инструмент?", "Задать вопрос пользователю?" или "Завершить работу?".
    
    - _Ключевое отличие:_ Инверсия контроля. В Chain вы управляете потоком, в Agent потоком управляет модель.
        


#### Вопрос: Почему цепочка (Chain) описывается как DAG, и какие ограничения из этого следуют?
**Ответ:**
Chain обычно задаётся как Directed Acyclic Graph: шаги соединены направлёнными рёбрами, но циклов нет, поэтому выполнение всегда конечное и заранее предсказуемое по структуре. Из этого следуют ограничения: нельзя естественно реализовать «попробуй ещё раз», «уточни у пользователя», «если ошибка инструмента — повтори» без дополнительных надстроек. Любая нестандартная ситуация (неожиданный формат ответа, недостающая информация) легко ломает выполнение, потому что цепочка не умеет менять путь в зависимости от состояния.

#### Вопрос: Что именно означает «инверсия контроля» при переходе к агенту, и как это влияет на дизайн системы?
**Ответ:**
Инверсия контроля означает, что решение «какой следующий шаг» принимает не разработчик, а модель, анализируя текущее состояние. Поэтому дизайн смещается: вместо того чтобы кодом прописывать каждый переход, вы проектируете среду принятия решений — state, набор инструментов и правила маршрутизации. Важно сделать наблюдения (results инструментов) явными, чтобы агент опирался на факты, а не на догадки, и добавить ограничители: лимиты шагов, ретраи, обработку ошибок и стратегии уточнения у пользователя.
### 1.2. Когнитивные Архитектуры

Агент — это не просто цикл. Это "мозг", работающий по определенному алгоритму.

#### A. ReAct (Reason + Act)

Самый популярный паттерн (Yao et al., 2022).

1. **Thought (Мысль):** Агент анализирует запрос. _"Пользователь просит погоду в Пензе. Мне нужен инструмент `get_weather`."_
    
2. **Action (Действие):** Агент формирует вызов инструмента. `get_weather(city="Penza")`.
    
3. **Observation (Наблюдение):** Среда выполняет код и возвращает результат. `Temperature: -15C`.
    
4. **Repeat:** Агент получает результат и снова думает. _"Данных достаточно. Ответ: В Пензе -15 градусов."_
    


#### Вопрос: Как Observation в ReAct помогает бороться с галлюцинациями и «самоуверенными» выводами модели?
**Ответ:**
Observation — это внешний сигнал от среды (результат инструмента), который должен считаться более надёжным, чем предположения модели. Когда агент явно отделяет действие (tool call) от наблюдения (tool result), он получает «якорь» реальности и может обновить своё понимание. Это снижает вероятность того, что модель продолжит придумывать факты, потому что в контексте появляется конкретный результат (например, температура), с которым нужно согласовать финальный ответ.

#### Вопрос: Какие типичные ошибки делает агент в цикле ReAct и как их ограничить на уровне графа/состояния?
**Ответ:**
Типично агент ошибается в выборе инструмента, в формировании аргументов или начинает «зацикливаться» на повторных вызовах без прогресса. Ограничивают это механизмами state и графа: лимит шагов (например, `steps_count`), проверка наличия `tool_calls` через conditional edge, ретраи только при определённых ошибках, а также fallback‑ветка «спросить пользователя», если данных недостаточно. Дополнительно помогает строгая schema инструментов и серверная валидация аргументов до выполнения.
#### B. Plan-and-Execute (Plan-and-Solve)

ReAct страдает "близорукостью" — он видит только следующий шаг. Для сложных задач (написание книги, анализ больших данных) нужен план.

1. **Planner:** Отдельная LLM генерирует список шагов (1. Найти данные, 2. Проанализировать, 3. Написать отчет).
    
2. **Executor:** Агент (или Worker) берет шаг №1, выполняет его, обновляет состояние.
    
3. **Replanner:** (Опционально) После выполнения шага план может быть скорректирован.
    


#### Вопрос: В чем «близорукость» ReAct и как планирование в Plan-and-Execute её компенсирует?
**Ответ:**
ReAct ориентирован на следующий шаг: он хорошо решает локальную подзадачу, но не всегда удерживает общую структуру многосоставной задачи. Из‑за этого агент может делать лишние действия, повторять поиск или не закрывать все требования. Plan-and-Execute компенсирует это вынесением глобального плана в явную структуру: сначала планировщик фиксирует шаги, затем исполнитель выполняет их по очереди, а прогресс измеряется завершённостью шагов.

#### Вопрос: Зачем нужен Replanner и что он должен учитывать при корректировке плана?
**Ответ:**
Replanner нужен, когда фактические наблюдения меняют ситуацию: инструмент вернул ошибку, данные оказались неполными, или пользователь уточнил требования. Хороший replanner смотрит на текущее состояние (что уже сделано, какие артефакты получены, сколько попыток было) и принимает решение: заменить шаг, добавить новый, поменять порядок или завершить задачу с пояснением. Важно, чтобы replanning запускался по триггерам (ошибки, отсутствие прогресса, новые ограничения), а не «каждый шаг заново».

#### Вопрос: Зачем вообще выделять «когнитивную архитектуру» агента, если можно просто делать loop с вызовами LLM?
**Ответ:**
Loop сам по себе не задаёт алгоритм поведения: без структуры агент может хаотично выбирать действия, повторяться или уходить в тупики. Когнитивная архитектура (например, ReAct или Plan-and-Execute) задаёт понятную схему: как формируется решение, как вызываются инструменты, как интерпретируются наблюдения и как измеряется прогресс. Это делает систему проще для отладки и тестирования: вы можете проверять не только финальный ответ, но и корректность промежуточных шагов.

#### Вопрос: Как выбрать между ReAct и Plan-and-Execute для конкретной задачи?
**Ответ:**
Если задача короткая и локальная (нужно один‑два вызова инструмента и быстрый ответ), ReAct обычно достаточно: «подумал → действовал → увидел результат». Если задача многосоставная и требует удерживать план из зависимых шагов (сбор данных, анализ, синтез отчёта), Plan-and-Execute даёт преимущество: отдельный планировщик фиксирует структуру, исполнитель выполняет шаги, а replanner корректирует план при изменениях. Практический критерий — наличие явных подзадач и риск «близорукости» (локальных решений, не ведущих к цели).
### 1.3. Tool Use (Function Calling)

Как LLM взаимодействует с внешним миром?  
Раньше (2022-2023) использовали MRKL и парсинг регулярками (`Action: Search\nInput: Penza`). Сейчас стандартом стал **Native Function Calling** (OpenAI, Anthropic).

1. **Schema:** Вы описываете инструменты в формате JSON Schema (автоматически из Pydantic моделей).
    
2. **Binding:** Вы "биндите" (привязываете) эти схемы к модели. `llm.bind_tools([weather_tool])`.
    
3. **Response:** Модель возвращает не текст, а структуру `tool_calls` (ID инструмента, аргументы).
    
4. **Execution:** Ваш Runtime (LangGraph) выполняет функцию и возвращает результат с тем же `tool_call_id`.
    

---


#### Вопрос: Почему Native Function Calling считается стандартом по сравнению с MRKL-подходом через парсинг текста?
**Ответ:**
MRKL‑подход зависит от того, что модель строго выведет текст в нужном формате (например, `Action: ... Input: ...`), и любая вариация ломает парсер. Native Function Calling переводит интеграцию в структурированный протокол: модель возвращает `tool_calls` с именем инструмента и аргументами, соответствующими schema. Это повышает надёжность, упрощает валидацию и делает выполнение инструментов более предсказуемым на уровне рантайма.

#### Вопрос: Какую роль играет `tool_call_id`, и почему важно возвращать результат именно с тем же идентификатором?
**Ответ:**
`tool_call_id` — это корреляция между запросом на выполнение инструмента и его результатом. Если агент сделал несколько вызовов или выполнение асинхронное, идентификатор позволяет однозначно сопоставить Observation нужному Action. Игнорирование `tool_call_id` приводит к путанице результатов, особенно в графах с параллелизмом или несколькими инструментами: агент может «увидеть» не то наблюдение и принять неверное решение на следующем шаге.

#### Вопрос: Что в этом хендбуке считается «агентом», и чем он принципиально отличается от обычного пайплайна LLM?
**Ответ:**
В контексте этого конспекта агент — это система, где LLM выступает «Reasoning Engine» и сама управляет потоком выполнения в цикле (loop): на каждом шаге оценивает состояние и решает, вызвать ли инструмент, задать ли уточняющий вопрос или завершиться. Обычный LLM‑пайплайн (chain) чаще всего — заранее зафиксированный DAG вызовов, где разработчик полностью задаёт порядок шагов. Главное отличие — не в количестве шагов, а в том, кто принимает решения о следующем действии: в chain это код, в agent — модель, опирающаяся на текущее состояние и наблюдения.

#### Вопрос: Какие «слои» нужно продумать, чтобы агентная система была управляемой и предсказуемой?
**Ответ:**
Даже если модель принимает решения, разработчик задаёт рамки: (1) какие инструменты доступны и их схемы (schema/binding), (2) какое состояние хранится и как оно мерджится (reducers), (3) как устроен контрольный поток (graph: узлы/рёбра/условная маршрутизация), (4) как обеспечивается долговечность (checkpointers, thread_id) и (5) как обрабатываются ошибки (ретраи, фолбэки). Управляемость появляется, когда решения модели ограничены контрактами (JSON Schema) и архитектурой графа, а не «свободным текстом».
## 2. LangGraph: Архитектура и примитивы

LangGraph решает главную проблему LangChain — отсутствие нормального управления состоянием (State Management) и сложность создания циклов.

### 2.1. State (Граф Состояний)

В центре всего — **State Schema**. Это `TypedDict` или `Pydantic` модель, описывающая "память" вашего графа.

```python
from typing import TypedDict, Annotated
from langgraph.graph.message import add_messages

class AgentState(TypedDict):
    # Самое важное поле. Хранит историю чата.
    # Annotated[list, add_messages] означает: "Когда узел возвращает новые сообщения,
    # НЕ перезаписывай список, а добавляй (append) их в конец".
    messages: Annotated[list, add_messages]
    # Кастомные поля (перезаписываются по умолчанию)
    user_id: str
    steps_count: int
```

**Reducer (`add_messages`):** Это функция, определяющая, как объединять старое и новое состояние. `add_messages` — это встроенный редьюсер, который умеет:

- Добавлять новые сообщения.
    
- Обновлять старые сообщения (если у них совпадает `id`). Это критично для исправления ошибок или Human-in-the-loop редактирования.
    


#### Вопрос: Почему поле `messages` считается «самым важным», и зачем ему нужен `Annotated[list, add_messages]`?
**Ответ:**
История сообщений — это контекст, на котором LLM принимает решения: без неё агент теряет память о запросах, tool calls и наблюдениях. `Annotated[list, add_messages]` задаёт редьюсер, который не перезаписывает список, а добавляет новые сообщения в конец. Это предотвращает случайную потерю истории при обновлениях узлов и делает накопление диалога стандартным поведением.

#### Вопрос: В каких случаях критично, что `add_messages` умеет обновлять старые сообщения по `id`?
**Ответ:**
Это важно для сценариев исправления: Human‑in‑the‑loop может отредактировать неправильный tool call или изменить аргументы, не ломая всю историю. Если редьюсер умел бы только append, вы бы получили две версии одного и того же сообщения и риск несогласованного контекста. Обновление по `id` позволяет «подменить» сообщение корректной версией, сохранив целостность state и облегчая time travel/state editing.
### 2.2. Nodes (Узлы)

Узлы — это функции Python. Они:

1. Принимают текущий `State`.
    
2. Выполняют работу (вызов LLM, вызов API, поиск в БД).
    
3. Возвращают**обновление** (Update) состояния.
    

```python
def chatbot_node(state: AgentState):
    # Логика: вызываем модель с инструментами
    response = llm_with_tools.invoke(state["messages"])    
    # Возвращаем дельту. Reducer сам добавит это в общий список.    
    return {"messages": [response]}
```


#### Вопрос: Почему узел возвращает именно «обновление» состояния, а не весь state целиком?
**Ответ:**
Возврат дельты снижает связность: узел отвечает только за те поля, которые он меняет, а объединение делает система через reducers. Это уменьшает риск случайно затереть другие части состояния и упрощает композицию: разные узлы могут независимо добавлять сообщения, обновлять счётчики и т.д. Такой подход также делает граф более масштабируемым и предсказуемым.

#### Вопрос: Какие практики помогают сделать узлы тестируемыми и переиспользуемыми?
**Ответ:**
Полезно держать узлы максимально «чистыми»: получать всё необходимое из state и возвращать только update, а внешние зависимости (LLM‑клиент, конфиг, инструменты) передавать через параметры сборки графа. Для тестов удобно мокать вызовы LLM и API, проверяя, что узел корректно формирует сообщения и обновления. Также важно логировать вход/выход узла или события стриминга, чтобы разбирать поведение на реальных трассах.
### 2.3. Edges (Ребра)

Ребра определяют переходы.

- **Normal Edge:** `builder.add_edge("node_a", "node_b")`. Всегда идти туда.
    
- **Conditional Edge:** `builder.add_conditional_edges("node_a", route_function)`.
    
    - Функция маршрутизации (`route_function`) смотрит на стейт (например, "есть ли в последнем сообщении `tool_calls`?") и возвращает имя следующего узла.
        


#### Вопрос: Зачем нужны Conditional Edges, если можно «внутри узла» просто вызвать следующий узел функцией?
**Ответ:**
Conditional edges отделяют логику маршрутизации от логики вычисления: узлы делают работу, а граф решает, куда идти дальше. Это позволяет централизованно управлять переходами, строить циклы и делать ветвления на основе state (например, проверять `tool_calls`). Кроме того, такая структура проще для отладки: видно, почему граф пошёл по конкретному пути, и это легче тестировать как отдельную `route_function`.

#### Вопрос: Какие сигналы чаще всего используют в `route_function` для агентного цикла ReAct?
**Ответ:**
Классический сигнал — наличие `tool_calls` в последнем сообщении модели: если они есть, идём в узел выполнения инструментов; если нет — завершаем (`__end__`) или идём в узел финального ответа. Дополнительно используют ошибки инструментов (по содержимому Observation), лимит шагов (`steps_count`), флаги «нужно уточнение у пользователя» и т.п. Это превращает граф в явный контроллер поведения агента.
### 2.4. Persistence (Память и Чекпоинты)

Агент в продакшене не живет в RAM. Он должен пережить перезагрузку сервера. LangGraph использует **Checkpointers**.

- **Как это работает:** После каждого выполнения узла (Super-step), LangGraph сохраняет `State` в базу данных.
    
- **Thread ID:** Уникальный идентификатор сессии. `config={"configurable": {"thread_id": "123"}}`.
    
- **PostgresSaver:** Стандарт для продакшена.
    
    - _Async Connection Pool:_ Используйте `psycopg_pool` для высокой нагрузки.
        
    - _JSONB:_ Стейт хранится в JSONB, что позволяет делать запросы к истории (например, "найти всех пользователей, кто спрашивал про цены").
        

---


#### Вопрос: Что такое Super-step в контексте LangGraph persistence, и почему чекпоинт делают именно после него?
**Ответ:**
Super‑step — это выполнение одного узла с применением его update к общему состоянию. Сохранение state после каждого такого шага гарантирует, что агент можно восстановить почти в любой точке: если сервер перезапустится, сессия продолжится с последнего сохранённого состояния. Это особенно важно для длинных диалогов и для сценариев, где агент делает много tool calls и переходов.

#### Вопрос: Почему для продакшена упоминается PostgresSaver и хранение state в JSONB?
**Ответ:**
Postgres — распространённая инфраструктура для продакшена, а JSONB удобно хранит структурированный state без жёсткой схемы на уровне таблиц. JSONB также позволяет делать запросы по содержимому state (например, находить пользователей по паттернам в истории). В связке с `thread_id` это даёт практичную модель: идентифицируем сессию, читаем/пишем её состояние и масштабируем API горизонтально.

#### Вопрос: Какие проблемы LangChain подразумеваются под «отсутствием нормального State Management», и как LangGraph их закрывает?
**Ответ:**
В классическом подходе цепочек сложно прозрачно хранить и обновлять состояние, а циклы и ветвления становятся громоздкими. LangGraph делает state центральным примитивом: каждый узел принимает state и возвращает update, а объединение выполняют reducers. Это упрощает циклы (например, ReAct), условные переходы и повторные попытки, потому что граф изначально рассчитан на управление состоянием и маршрутизацию по нему.

#### Вопрос: Почему графовая модель удобна именно для агентных систем, а не только для «визуализации пайплайна»?
**Ответ:**
Потому что граф в LangGraph — это исполняемая модель управления: узлы — вычисления, рёбра — переходы, conditional edges — логика выбора следующего шага. Агентные системы требуют циклов, прерываний, повторов и ветвлений на основе наблюдений, а это естественно выражается через граф. При этом state сохраняется между super‑steps и может быть восстановлен через checkpointer, что критично для продакшена.
## 3. Продвинутые паттерны

### 3.1. Human-in-the-Loop (HITL)

ИИ может ошибаться. Иногда нужен человек, чтобы подтвердить действие (Approval) или исправить данные (Edit).

#### A. Breakpoints (Точки останова)

Мы можем сказать графу: "Остановись перед выполнением узла `execute_payment`".

```python
graph = builder.compile(interrupt_before=["execute_payment"])
```

1. Агент доходит до узла и встает на паузу.
    
2. Мы получаем уведомление.
    
3. Мы проверяем стейт: `snapshot = graph.get_state(config)`.
    


#### Вопрос: Как работает `interrupt_before` и что именно получает разработчик на паузе?
**Ответ:**
`interrupt_before` говорит графу остановиться прямо перед выполнением указанного узла. Когда выполнение доходит до этой точки, система не делает действие, а возвращает управление, позволяя получить snapshot состояния через `graph.get_state(config)`. Это даёт возможность посмотреть, какие сообщения накоплены, какие действия запланированы, и принять решение: продолжать, отменить или отредактировать state.

#### Вопрос: Какие узлы имеет смысл защищать breakpoints в продакшене?
**Ответ:**
Обычно это узлы с побочными эффектами: платежи (`execute_payment`), отправка писем/уведомлений, операции с аккаунтами, изменения цен/настроек и любые действия, где ошибка дорого стоит. Breakpoints также полезны на «границе доверия»: когда агент собрал данные и собирается выполнить критическое действие, человек может проверить корректность перед продолжением.
#### B. Time Travel & State Editing

Если агент сформировал неправильную транзакцию, мы не просто отклоняем её. Мы можем **исправить** стейт.


```python
# Получаем последнее сообщение с вызовом инструмента
last_msg = snapshot.values["messages"][-1]
# Меняем аргументы (например, сумму платежа) 
last_msg.tool_calls[0]["args"]["amount"] = 100 
# Обновляем стейт в базе 
graph.update_state(config, {"messages": [last_msg]})
# Продолжаем выполнение 
graph.invoke(None, config=config)
 ```


#### Вопрос: Что в примере означает «исправить стейт», и почему это лучше, чем просто отклонить транзакцию?
**Ответ:**
В примере агент сформировал неправильные аргументы tool call (например, сумму платежа). Вместо того чтобы отклонить действие и начинать диалог заново, вы берёте snapshot, находите последнее сообщение с вызовом инструмента, меняете нужные поля (например, `amount`) и обновляете state через `graph.update_state`. Это сохраняет весь контекст и позволяет продолжить выполнение с исправленными данными, экономя время и токены.

#### Вопрос: Какие риски появляются при ручном редактировании `messages` и как их минимизировать?
**Ответ:**
Риск — нарушить согласованность истории: если изменить сообщение без сохранения структуры `tool_calls` или без корректных `id`, редьюсер и логика маршрутизации могут работать неправильно. Минимизируют риски через обновление по `id`, валидацию структуры сообщения перед записью, логирование правок и ограничение прав на редактирование. Полезно также фиксировать причину правки (audit trail), чтобы понимать, почему параметры изменились.

#### Вопрос: Какие два основных типа вмешательства человека выделены в разделе HITL и в чем разница?
**Ответ:**
Здесь выделены Approval (подтверждение действия) и Edit (исправление данных/состояния). Approval нужен, когда действие потенциально рискованно или необратимо: человек даёт «ок» перед выполнением. Edit нужен, когда агент уже сформировал неверные данные (например, аргументы транзакции) — человек может отредактировать state и продолжить выполнение без полного перезапуска диалога.

#### Вопрос: Почему HITL особенно важен для инструментов, которые изменяют внешний мир, а не просто читают данные?
**Ответ:**
Чтение данных (поиск, получение погоды) обычно обратимо: ошибка ведёт к неверному ответу, но не меняет систему. Действия, которые меняют внешний мир (платежи, создание задач, изменения настроек), несут риск ущерба. HITL вводит контрольную точку: даже если LLM ошиблась, человек может остановить выполнение или скорректировать параметры до выполнения инструмента.
### 3.2. Multi-Agent Systems (MAS) - Supervisor Pattern

Один агент не может уметь всё. Мы создаем команду специалистов.

- **Supervisor (Начальник):** LLM, которой дали инструменты-агенты (`Researcher`, `Coder`, `Writer`).
    
- **Логика:** Supervisor видит задачу "Напиши отчет про Apple". Он вызывает `Researcher`. Тот работает (ходит в интернет), возвращает текст. Supervisor передает этот текст `Writer`.
    
- **Shared State vs Isolated State:**
    
    - _Shared:_ Все агенты видят всю историю переписки (просто, но замусоривает контекст).
        
    - _Isolated (Subgraphs):_ `Researcher` — это отдельный граф. Он получает на вход только задачу ("Найди данные за 2024 год"), работает в своем контексте, и возвращает только результат. Главный граф не видит промежуточных "мыслей" ресерчера. Это экономит токены и делает систему чище.
        


#### Вопрос: В чем смысл роли Supervisor и почему не сделать одного «универсального» агента?
**Ответ:**
Один универсальный агент либо будет слишком общим (и ошибаться), либо потребует огромного контекста и множества инструментов, что ухудшает точность и увеличивает стоимость. Supervisor позволяет разделить ответственность: он принимает задачу верхнего уровня и делегирует подзадачи специализированным агентам (`Researcher`, `Coder`, `Writer`). Это повышает качество, потому что каждый агент оптимизирован под свой тип работы, а Supervisor собирает результат в единый ответ.

#### Вопрос: Когда стоит выбирать Shared State, а когда Isolated State (Subgraphs) для MAS?
**Ответ:**
Shared state проще: все агенты видят всю историю и могут опираться на общий контекст, но это быстро замусоривает контекст и повышает стоимость. Isolated state (subgraphs) изолирует выполнение: агент получает только подзадачу и возвращает итог, а промежуточные шаги остаются внутри подграфа. Такой подход экономит токены и делает систему чище, особенно когда специализированные агенты выполняют много внутренних действий.
### 3.3. Dynamic Graph Construction (Динамические графы)

Иногда мы не знаем структуру графа заранее.

- _Пример:_ Пользователь загружает 5 файлов. Нужно создать 5 параллельных веток обработки.
    
- **Send API (Map-Reduce):** Вместо фиксированных ребер, узел может вернуть объект `Send("node_name", arg)`. Это позволяет динамически спавнить параллельные задачи.
    

---


#### Вопрос: Зачем нужны динамические графы, если можно заранее описать все узлы и рёбра?
**Ответ:**
Иногда структура задачи известна только во время выполнения: например, пользователь загрузил N файлов, и нужно обработать каждый файл отдельной веткой. Если граф фиксированный, вам пришлось бы заранее закладывать максимум веток или усложнять логику узлов. Динамическое построение через Send API позволяет запускать нужное число задач по факту входных данных, сохраняя архитектуру аккуратной.

#### Вопрос: Как работает Send API в стиле Map-Reduce и что он даёт агентной системе?
**Ответ:**
Идея Map‑Reduce: на этапе map вы запускаете параллельные подзадачи (например, обработку каждого файла), а затем на reduce собираете результаты в единый вывод. В LangGraph узел может вернуть объект `Send("node_name", arg)`, что позволяет динамически «спавнить» ветки выполнения. Это помогает распараллеливать I/O‑bound задачи и сокращать латентность, не теряя преимуществ stateful‑управления.

#### Вопрос: Что отличает «продвинутые паттерны» от базовых примитивов вроде state/nodes/edges?
**Ответ:**
Базовые примитивы — это строительные блоки графа, а продвинутые паттерны — устойчивые решения типовых продакшен‑проблем: контроль человеком (HITL), команда агентов (MAS), динамическое построение веток (Send API) и т.д. Они отвечают на вопросы «как управлять риском» и «как масштабировать поведение», а не просто «как соединить узлы».

#### Вопрос: Какие паттерны чаще всего комбинируют в реальных системах и почему?
**Ответ:**
Часто комбинируют ReAct + HITL: агент действует через инструменты, но перед опасными шагами ставят breakpoints/approval. Для сложных задач добавляют supervisor pattern: один управляющий агент делегирует подзадачи специализированным. Если входные данные вариативны (например, много файлов), подключают dynamic graph construction через Send API, чтобы параллелить обработку и затем собирать результат.
## 4. Инженерия и продакшн

### 4.1. Streaming (Потоковая передача)

UX чат-бота требует мгновенного отклика.

1. **Stream Messages:** `.stream(..., stream_mode="messages")`. Просто чанки текста.
    
2. **Stream Events (v2):** Самый мощный режим. Позволяет строить UI, который показывает:
    
    - 🤔 _Thinking..._ (on_chat_model_start)
        
    - 🛠️ _Calling Tool 'Google Search'..._ (on_tool_start)
        
    - 📄 _Reading Results..._ (on_tool_end)
        
    - 📝 _Typing Answer..._ (on_chat_model_stream)
        
3. **Custom Events:** Вы можете сами эмитить события из узлов (`adispatch_custom_event`), чтобы показывать прогресс-бары или статусы на фронтенде.
    


#### Вопрос: Чем принципиально отличается `.stream(..., stream_mode="messages")` от режима событий `stream_mode="events" (v2)`?
**Ответ:**
`messages` обычно отдаёт только текстовые чанки ответа — этого достаточно для эффекта «бот печатает». `events` (v2) отдаёт структурированные события жизненного цикла: старт модели, начало/конец вызова инструмента, поток токенов, а также кастомные события. Это позволяет строить более богатый UI: показывать статусы «Thinking…», «Calling Tool…», «Reading Results…», а не только финальный текст.

#### Вопрос: Зачем нужны Custom Events и где они дают максимальную пользу?
**Ответ:**
Custom events позволяют эмитить доменные сигналы прямо из узлов (например, `adispatch_custom_event`): «обрабатываю файл 3/5», «построил план из 7 шагов», «валидирую аргументы». На фронтенде это можно визуализировать как прогресс‑бар, шаги мастера или статусную ленту. Польза максимальна в длинных задачах, где без объяснения этапов пользователь воспринимает задержку как «бот завис», а события делают прогресс видимым.
### 4.2. Error Handling & Fallbacks

В продакшене LLM галлюцинируют, а API падают.

1. **Tool Level:** Оберните код инструмента в `try/except`. При ошибке возвращайте строку: `"Error: Service unavailable. Try again."`. LLM прочитает это и, возможно, попробует другой инструмент.
    
2. **Graph Level:** Если узел падает с исключением, граф останавливается. Используйте `retry policies` (в LangGraph есть встроенные ретраи).
    
3. **Fallback Models:** Если `gpt-4` упала по Rate Limit, переключитесь на `gpt-3.5-turbo` или `claude-haiku` в блоке `except`.
    


#### Вопрос: Почему на уровне инструмента советуют `try/except` и возврат строки ошибки, а не падение исключением?
**Ответ:**
Если инструмент падает исключением и оно не обработано, выполнение узла может прервать весь граф. Возвращая понятную строку (например, `"Error: Service unavailable. Try again."`) вы превращаете сбой в Observation, которую LLM может прочитать и принять решение: повторить попытку, выбрать другой инструмент или уточнить входные данные. Это повышает устойчивость ReAct‑цикла и снижает количество «жёстких» остановок.

#### Вопрос: Как сочетать retry policies и fallback models, чтобы не уйти в бесконечные ретраи и рост стоимости?
**Ответ:**
Нужно задавать лимиты и классификацию ошибок: сколько раз ретраить конкретный узел/инструмент и какие ошибки ретраибельны (например, временная недоступность, но не «invalid args»). После исчерпания лимита можно переключаться на fallback model (другая модель при rate limit) или менять стратегию (спросить пользователя, завершить с объяснением). Важно логировать ретраи и переключения, чтобы затем улучшать промпты, схемы инструментов и инфраструктуру.
### 4.3. Evaluation & Testing

Как тестировать агентов?

1. **Unit Tests (Deterministic):** Тестируем отдельные инструменты и функции парсинга.
    
2. **Integration Tests (Simulated):** Запускаем агента с моками инструментов (Mock Tools). Проверяем, что на вопрос "Погода в Пензе" агент вызывает `get_weather("Penza")`, а не `get_news("Penza")`.
    
3. **LangSmith:** Платформа для трассировки. Позволяет записывать реальные сессии пользователей, аннотировать их (Like/Dislike) и добавлять в датасеты для тестирования.
    


#### Вопрос: Что именно проверяют Integration Tests для агентов помимо корректности текста ответа?
**Ответ:**
Интеграционные тесты проверяют поведение системы как контроллера действий: какие инструменты вызываются, с какими аргументами, как агент реагирует на Observation и ошибки. Пример из текста — убедиться, что на «Погода в Пензе» агент вызывает `get_weather("Penza")`, а не `get_news("Penza")`. Это критично, потому что агентный ответ может быть «красивым», но неверным по действиям (не те источники, не те шаги).

#### Вопрос: Почему LangSmith полезен именно для агента в графе, а не только для одиночного вызова LLM?
**Ответ:**
Для агента важно видеть трассу: последовательность узлов, tool calls, Observations, маршруты conditional edges и финальный вывод. LangSmith помогает записывать реальные сессии, аннотировать качество и собирать датасеты для регрессионного тестирования. Это даёт возможность отслеживать, как изменения в промптах, инструментах или модели влияют на выбор действий и на устойчивость, а не только на стиль текста.
### 4.4. Deployment (Docker & Scaling)

- **LangGraph API (LangGraph Cloud/Server):** Библиотека `langgraph-cli` позволяет поднять готовый REST API сервер (`langgraph up`). Он уже умеет работать с Postgres, очередями задач и асинхронностью.
    
- **Docker:** Оборачиваем сервер в контейнер.
    
- **Scaling:** Агенты — это I/O bound задачи (ждем LLM). Используем `asyncio` и запускаем воркеры. Состояние хранится в Postgres, поэтому можно горизонтально масштабировать API-серверы.
    

---


#### Вопрос: Что практично даёт LangGraph API / `langgraph-cli` при деплое и почему это снижает риски?
**Ответ:**
Это даёт готовую серверную обвязку: вы поднимаете REST API (`langgraph up`) вместо того, чтобы вручную реализовывать маршрутизацию запросов, управление сессиями, интеграцию с Postgres и асинхронность. В результате меньше инфраструктурного кода, меньше ошибок в интеграции и быстрее путь от графа к продакшен‑сервису. Плюс проще стандартизировать деплой через Docker.

#### Вопрос: Почему горизонтальное масштабирование проще, когда state хранится в Postgres, и какие нюансы нужно учесть?
**Ответ:**
Если state хранится в общей БД, любой инстанс API может продолжить выполнение по `thread_id`, и сессия не привязана к одному процессу. Нюансы — конкуррентность и нагрузка: нужно следить, чтобы два воркера не конфликтовали при обновлении одного и того же состояния, использовать пулы соединений и мониторить частоту чекпоинтов (они происходят после super‑step). Также важно продумать таймауты и идемпотентность инструментов, чтобы ретраи не приводили к двойным действиям.

#### Вопрос: Какие продакшен-требования чаще всего «ломают» прототип агента, если их не учесть заранее?
**Ответ:**
Чаще всего ломают: стриминг (нужен быстрый UX), устойчивость к падениям внешних сервисов (ошибки инструментов, rate limits), долговечная память (перезапуски серверов), наблюдаемость (трассировка, метрики) и тестирование (регрессии при смене моделей/промптов). Прототип может работать в идеальных условиях, но без чекпоинтов, ретраев и фолбэков в продакшене быстро становится нестабильным.

#### Вопрос: Почему агенты часто называют I/O bound и как это влияет на архитектуру деплоя?
**Ответ:**
Агенты большую часть времени ждут внешние ответы: LLM API, поиск, БД, инструменты. Это означает, что производительность упирается не в CPU, а в ожидание ввода‑вывода. Поэтому выгодно использовать асинхронность (`asyncio`), пулы соединений (например, для Postgres) и горизонтальное масштабирование воркеров. При хранении state в общей БД можно масштабировать API‑серверы, потому что «память» не привязана к конкретному процессу.
## ИТОГОВАЯ СВОДКА (CHEAT SHEET)

### Основные компоненты

|Компонент|Назначение|Python|
|---|---|---|
|**State**|Память агента|`class State(TypedDict): messages: Annotated[list, add_messages]`|
|**Node**|Функция-обработчик|`def node(state): return {"key": "value"}`|
|**Edge**|Переход|`graph.add_edge("a", "b")`|
|**Conditional Edge**|Логика if/else|`graph.add_conditional_edges("a", router_func, {"key": "node"})`|
|**Checkpointer**|Сохранение в БД|`PostgresSaver`, `SqliteSaver`|
|**Interrupt**|Пауза для человека|`compile(interrupt_before=["step"])`|


#### Вопрос: Почему в таблице выделены именно State/Node/Edge/Conditional Edge/Checkpointer/Interrupt как базовый набор?
**Ответ:**
Этот набор покрывает полный цикл исполнения: State хранит память, Node выполняет вычисление и возвращает update, Edge задаёт поток выполнения, Conditional Edge добавляет if/else логику, Checkpointer делает состояние долговечным, а Interrupt позволяет безопасно остановить выполнение для человека. Вместе они дают минимальную архитектуру, на которой можно собрать агентный цикл с инструментами и контролем.

#### Вопрос: Как на практике связаны State и Reducer, и почему редьюсер важнее, чем кажется из примера?
**Ответ:**
State задаёт структуру памяти, а reducer определяет правила объединения старого и нового состояния. Для сообщений это критично: без `add_messages` узлы могли бы перезаписать историю, потеряв контекст. Редьюсер также позволяет целевым образом обновлять прошлые сообщения по `id`, что нужно для HITL‑редактирования. То есть reducer — это «семантика памяти», которая напрямую влияет на корректность поведения агента.
### Типичная архитектура ReAct с HITL



```python
# 1. State
class AgentState(TypedDict):
     messages: Annotated[list, add_messages]

# 2. Nodes 
def agent(state):     # LLM решает, что делать
    return {"messages": [llm.invoke(state["messages"])]}
def tools(state):     # Выполнение инструментов    
	return {"messages": [tool_executor.invoke(state["messages"][-1])]} 
	
# 3. Router 
def route(state):     
	if state["messages"][-1].tool_calls:        
		return "tools"    
	return "__end__" 
	
# 4. Graph 
workflow = StateGraph(AgentState) 
workflow.add_node("agent", agent) 
workflow.add_node("tools", tools) 
workflow.set_entry_point("agent") 
workflow.add_conditional_edges("agent", route) 
workflow.add_edge("tools", "agent")

# Цикл: после тула снова думаем 
# 5. Compile with Memory 
checkpointer = MemorySaver() 
app = workflow.compile(checkpointer=checkpointer, interrupt_before=["tools"])
```


#### Вопрос: Как в примере реализуется цикл ReAct, и где именно происходит «Act»?
**Ответ:**
Цикл создаётся ребром `workflow.add_edge("tools", "agent")`: после выполнения инструмента граф возвращается к узлу `agent`, который снова вызывает LLM. «Act» происходит в узле `tools`, где выполняется `tool_executor.invoke(...)` для последнего сообщения с `tool_calls`. Узел `route` контролирует переход: если `tool_calls` есть — идём в `tools`, иначе завершаем.

#### Вопрос: Зачем в примере используется `interrupt_before=["tools"]` и как это превращает цикл в HITL?
**Ответ:**
Так граф ставит паузу прямо перед исполнением инструментов: модель может предложить действие, но его выполнение требует проверки человеком. Это особенно полезно, если инструменты имеют побочные эффекты или важны деньги/безопасность. Таким образом ReAct (решение модели о следующем шаге) комбинируется с HITL (approval/edit) на самой рискованной точке — перед «Act» в реальном мире.
### Когда использовать LangGraph?

- ✅ Вам нужны **циклы** (агент пробует, ошибается, исправляет).
    
- ✅ Вам нужна **память** (диалог длится дни).
    
- ✅ Вам нужен **контроль** (вы хотите четко знать, что происходит на каждом шаге).
    
- ❌ Если у вас простой RAG (вопрос-ответ), LangChain Chain или просто вызов OpenAI API будет проще.


#### Вопрос: Почему в критериях использования LangGraph выделены циклы, память и контроль как основные причины?
**Ответ:**
Потому что LangGraph сильнее всего там, где нужно управляемое итеративное поведение: агент должен пробовать, получать наблюдения, исправляться и повторять, а это требует циклов и conditional routing. Память нужна для долгих диалогов и восстановления после перезапусков через checkpointers и `thread_id`. Контроль означает наблюдаемость и предсказуемость на каждом шаге графа, чего трудно добиться в «склеенных» цепочках.

#### Вопрос: Почему для простого RAG часто достаточно chain или прямого вызова API, и что добавляет (и усложняет) LangGraph?
**Ответ:**
В простом RAG чаще всего нет сложных циклов и ветвлений: вопрос → извлечение → ответ. LangGraph добавляет инфраструктуру state, узлов/рёбер, маршрутизации, persistence и обработку ошибок на уровне графа. Это даёт мощь и контроль, но повышает сложность и стоимость поддержки. Поэтому LangGraph оправдан, когда действительно нужны циклы, длительная память и строгий контроль действий, а не просто линейный пайплайн.

#### Вопрос: Как использовать эту сводку: как справочник синтаксиса или как чеклист архитектуры?
**Ответ:**
Удобнее как чеклист: перед продакшен‑запуском пройтись по ключевым компонентам — есть ли State schema, Nodes, Edges/Conditional Edges, чекпоинтер, interrupts/HITL. Как справочник она помогает быстро вспомнить, как выглядит объявление state с `Annotated[list, add_messages]` или добавление conditional edges. Но главная ценность — убедиться, что у системы есть контроль, память и устойчивость.

#### Вопрос: Какие элементы сводки наиболее критичны именно для агентных циклов, а не для простого Q&A?
**Ответ:**
Для агентных циклов критичны conditional routing (ветвление по `tool_calls`), reducers для корректного мерджа истории (`add_messages`), persistence через checkpointer и возможность interrupts для контроля действий. Простой Q&A может обойтись без циклов и сложной маршрутизации, но агент, который пробует/ошибается/исправляет, без этих элементов быстро становится нестабильным и трудноотлаживаемым.
